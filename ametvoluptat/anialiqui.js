function domainSortDropped(sort) {
    if (sort === 'ascending') {
        // Sort the dropped items in ascending order
        // Example sorting logic:
        // droppedItems.sort((a, b) => a.domain.localeCompare(b.domain));
    } else if (sort === 'descending') {
        // Sort the dropped items in descending order
        // Example sorting logic:
        // droppedItems.sort((a, b) => b.domain.localeCompare(a.domain));
    } else {
        // Handle other sorting cases or provide a default behavior
    }
}
